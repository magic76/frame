export enum ECookieName {
	LANG = 'latnsg',
	DEFAULT_STAKE = 'dfstkd',
	TOKEN = 'toskwxj',
}

export const ERegex = {
	NUMBER: /^(\d*\.?\d*)$/,
};

export enum ESport {
	all = 0,
	football = 1,
	tennis = 2,
	cricket = 4,
	esport = 10,
	basketball = 21,
}

export enum EDateFormat {
	full = 'YYYY/MM/DD HH:mm:ss',
	date = 'YYYY/MM/DD',
}
